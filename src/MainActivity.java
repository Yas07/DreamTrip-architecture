
import Trip_DBs.Trips_BD;

import java.util.Comparator;

class MainActivity{
    public static void main(String[] args){
//		 Trips_BD trips_bd = new Trips_BD(Comparator.naturalOrder(), Comparator.naturalOrder(), Comparator.naturalOrder());

        // Trips_DB trips_db = new Trips_DB(comparator);


//        trips_bd.add(trip1.getStartDate(), trip1);
//        trips_bd.add(trip2.getStartDate(), trip2);
        //System.out.println(trips_bd.find("title"));
        //for (Trip_Items.Trips_trip iTrip:trips_bd) {
        //     System.out.println(iTrip);
        // }

        //TreeSet<Trip_Items.Trips_trip> set = new TreeSet<Trip_Items.Trips_trip>(comparator);

    }
}

